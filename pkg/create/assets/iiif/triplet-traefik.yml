http:
  services:
    triplet:
      loadBalancer:
        servers:
          - url: {{UPSTREAM_URL}}
  routers:
    triplet:
      rule: Host(`{{ env "DOMAIN" }}`) && PathPrefix(`/iiif`)
      service: triplet
      tls: {}
